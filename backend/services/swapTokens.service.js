const checkPoolBalanceService = () => {
  return "Checking your pool balance...📡";
};

const executeSwapService = (tokens, distributionFunds) => {
  // swap(tokens[0])
  return "SWAP: Executing swap...🔄";
};

module.exports = {
  checkPoolBalanceService,
  executeSwapService,
};
